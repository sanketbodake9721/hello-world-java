name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
    
jobs:
  build-container-image:
    runs-on: ubuntu-latest
    steps:
    - name: 'Checkout GitHub Action'
      uses: actions/checkout@main
      
    - uses: actions/upload-artifact@v3
      with:
           name: my-code-artifact
           path: HelloWorld.java
           
    - name: Login to JFrog 🐸  
      uses: docker/login-action@v1
      with:
          registry: bwapp.jfrog.io
          username: sanket.bodake@isteer.com
          password: Sank@2197
          run: curl -usanket.bodake@isteer.com:cmVmdGtuOjAxOjE3MDYwODAxOTk6Q3lVQVFYOER0SDZBNHNRNUdQejlSdEFkVHM2 -T HelloWorld.java "https://bwapp.jfrog.io/artifactory/example-repo-local/HelloWorld.java>"        
  
